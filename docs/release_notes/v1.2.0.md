# v1.2.0 更新内容

## Features

- 新增“关于”页面，可集中查看版本信息、项目链接和常用入口。
- 新增后端空闲管理，桌面端在空闲场景下可以更智能地控制后端运行状态。

## Improvements

- 调整微信传输层接入方式，后续扩展新的接入能力时会更统一。

## Breaking Changes

- 调整传输层扩展接口，仍在自定义接入 `BaseTransport` 的开发者需要改用 `poll_new_messages`、`send_text` 和 `send_files` 三个新方法，旧方法已不再兼容。
